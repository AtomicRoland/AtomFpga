MEMORY
{
   CODESEG:  start = $0600, size = $01FE, fill = yes, fillval = $ff;
   DATASEG:  start = $07FE, size = $0002, fill = yes, fillval = $ff;
}

SEGMENTS
{
   CODE:   load = CODESEG, type = ro;
   DATA:   load = DATASEG, type = ro;
}
